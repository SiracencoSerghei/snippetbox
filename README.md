# Snippetbox

Snippetbox — це навчальний веб-додаток на Go, створений для вивчення
структури Go-проєктів, HTTP-серверів, шаблонів та роботи з базою даних.

Проєкт розробляється за мотивами книги  
**“Let’s Go” — Alex Edwards**.

---

## 📦 Вимоги

- Go **>= 1.18**
- Git
- (пізніше) MySQL / PostgreSQL

Перевірка:

```bash
go version
```

---

## ⚡ Встановлення та запуск

### 🚀 Швидкий старт (рекомендовано)

⚠️ Для розробки **НЕ використовуй `go install`**.

```bash
git clone https://github.com/SiracencoSerghei/snippetbox.git
cd snippetbox
go run .
```

Після запуску відкрий у браузері:

```
http://localhost:4000
```

---

## 🛠️ Структура проєкту

```text
snippetbox/
├── cmd/
│   └── web/
│       └── main.go        # Точка входу
├── internal/
│   ├── models/            # Робота з БД
│   └── handlers/          # HTTP handlers
├── ui/
│   ├── html/              # Templates
│   └── static/            # CSS / JS
├── go.mod
└── README.md
```

---

## ▶️ Запуск з параметрами

```bash
go run ./cmd/web
```

(це стандартна практика в Go-проєктах)

---

## 🧪 Корисні команди

```bash
go vet ./...
go test ./...
go build ./cmd/web
```

---

## 📦 Збірка бінарника

```bash
go build -o snippetbox ./cmd/web
./snippetbox
```

---

## 📌 Встановлення (CLI / продакшн)

⚠️ Використовуй `go install` **тільки коли проєкт стабільний**.

```bash
go install github.com/SiracencoSerghei/snippetbox/cmd/web@latest
```

Переконайся, що:

```bash
$HOME/go/bin
```

доданий у змінну середовища `PATH`.

---

## 🔁 Коли переходити з `go run` → `go install`

### 🟢 `go run` — режим розробки

Використовуй, коли:

- пишеш код
- часто міняєш файли
- дебажиш
- навчаєшся

```bash
go run .
go run ./cmd/web
```

✔ не створює бінарник  
✔ ідеально для навчання

---

### 🟡 `go build` — перевірка

Використовуй, коли:

- хочеш перевірити, що проєкт збирається
- перед комітом
- перед деплоєм

```bash
go build ./cmd/web
```

✔ створює локальний бінарник  
✔ не встановлює в систему

---

### 🔵 `go install` — фінал

Використовуй, коли:

- проєкт стабільний
- хочеш запускати `snippetbox` з будь-якої директорії
- це CLI або продакшн-сервіс

```bash
go install github.com/SiracencoSerghei/snippetbox/cmd/web@v1.0.0
```

✔ встановлює в `$HOME/go/bin`  
✔ вимагає коректний `package main`

---

## 🧠 Золоте правило Go

> ❌ `go install` — **не для розробки**  
> ✅ `go run` — **для щоденної роботи**

---

## 📚 Посилання

- https://go.dev/doc/
- https://lets-go.alexedwards.net/

---

## 🧠 Примітка

Цей проєкт створений з навчальною метою для глибокого розуміння Go.

🧩 Крок 1: Перша ітерація веб‑додатку

На цьому етапі ми створюємо мінімальну, але повністю робочу веб‑аплікацію. Вона складається з трьох обовʼязкових компонентів:

1️⃣ Handler

Handler відповідає за:

виконання логіки застосунку;

формування HTTP‑відповіді (status code, headers, body).

Якщо ти знайомий з MVC(Model – View – Controller), handler можна уявляти як controller.

2️⃣ Router (ServeMux)

Router (у Go — http.ServeMux) зберігає відповідність між:

URL‑шляхами (наприклад /, /snippet/view)

відповідними handler‑ами.

Зазвичай застосунок має один ServeMux, у якому описані всі маршрути.

3️⃣ Web server

Go дозволяє запускати веб‑сервер без зовнішніх серверів (Nginx, Apache). HTTP‑сервер створюється і запускається безпосередньо в main.go.

```
cd $HOME/code/snippetbox 
$ go run main.go
$ go run .
$ go run github.com/SiracencoSerghei/snippetbox
```
Після запуску відкрий у браузері:

```http://localhost:4000
```
