# Чому НЕ МОЖНА використовувати float для грошей у Go

## Коротка відповідь
`float32` і `float64` **не здатні точно представляти десяткові дроби**, тому вони **НЕ підходять для фінансових розрахунків**.

Навіть мінімальна похибка в грошах = **реальні втрати**.

---

## 1. Проблема двійкового представлення

Компʼютери працюють у **двійковій системі**, а десяткові дроби (0.1, 0.2, 0.01) **не мають точного двійкового представлення**.

```go
fmt.Println(0.1 + 0.2)
```
Результат:
```
0.30000000000000004
```

Це **НЕ баг Go**, а фундаментальне обмеження IEEE‑754.

---

## 2. Реальний приклад з грошима

```go
price := 0.1
total := price * 3
fmt.Println(total)
```

Очікуємо:
```
0.3
```

Фактично:
```
0.30000000000000004
```

👉 Для одного платежу це дрібниця.  
👉 Для **мільйонів транзакцій** — катастрофа.

---

## 3. Накопичення похибки

```go
balance := 0.0

for i := 0; i < 1000; i++ {
    balance += 0.01
}

fmt.Println(balance)
```

Очікуємо:
```
10.00
```

Фактично:
```
9.999999999999831
```

📉 Помилка **накопичується**.

---

## 4. Правильний підхід: int64 у мінімальних одиницях

### 4.1 Зберігання грошей у копійках

```go
balance := int64(1000) // 10.00 грн = 1000 копійок
balance += 25          // +0.25 грн
balance -= 75          // -0.75 грн

fmt.Println(balance)   // 950
```

✔ Абсолютна точність  
✔ Жодних округлень  
✔ Безпечно для фінансів

---

### 4.2 Форматування для виводу

```go
amount := int64(12345)

fmt.Printf("%.2f
", float64(amount)/100)
```

Результат:
```
123.45
```

⚠️ `float` використовується **ЛИШЕ для відображення**, не для обчислень.

---

## 5. Валюти з іншою точністю

| Валюта | Мінімальна одиниця |
|------|-------------------|
| USD | 1 цент (×100) |
| EUR | 1 цент (×100) |
| UAH | 1 копійка (×100) |
| JPY | без дробів (×1) |
| BTC | 1 сатоші (×100,000,000) |

```go
type Money struct {
    Amount   int64
    Currency string
    Scale    int64
}
```

---

## 6. Коли float МОЖНА використовувати

✔ статистика  
✔ графіки  
✔ фізичні обчислення  
✔ машинне навчання  

❌ рахунки  
❌ баланси  
❌ податки  
❌ бухгалтерія  

---

## 7. Альтернатива: decimal бібліотеки

Якщо потрібна:
- довільна точність
- складні фінансові формули

📦 Популярні бібліотеки:
- `shopspring/decimal`
- `cockroachdb/apd`

❗ Повільніші, але точні.

---

## 8. Порівняння підходів

| Підхід | Точність | Швидкість | Рекомендація |
|-----|--------|---------|-------------|
| float64 | ❌ | 🔥 | НІ |
| int64 (копійки) | ✅ | 🔥🔥🔥 | ТАК |
| decimal | ✅ | 🐢 | За потреби |

---

## 9. Золоте правило

> **Float — для наближених значень.  
> Гроші — це точні значення.**

---

Автор: Serghei Siracenco
