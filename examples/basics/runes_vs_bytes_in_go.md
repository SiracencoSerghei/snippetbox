# Runes vs Bytes у Go

Go працює з рядками інакше, ніж багато старіших мов програмування.
Розуміння різниці між **байтами** та **рунами** є критично важливим
під час роботи з Unicode, емодзі та нелатинськими символами.

---

## Рядки в Go

У Go тип `string` — це **незмінна послідовність байтів**.

```go
s := "Hello"
fmt.Println(len(s)) // 5 байтів
```

Це працює добре для ASCII-символів, де
1 символ = 1 байт.
___

## Проблема мислення в стилі ASCII

У мовах на кшталт C символ зазвичай займає один байт
і використовує ASCII-кодування (7 біт = 128 символів).

Цього достатньо для англійської мови, але недостатньо для Unicode.

Приклади символів, які не вміщуються в один байт:

- Емодзі 😃

- Китайські ієрогліфи 你好

- Літери з діакритикою é, ü, ñ

Такі символи займають більше одного байта.
__

## Кодування UTF-8

Go використовує UTF-8 — кодування змінної довжини.

| Символ | Байти |
| ------ | ----- |
| A      | 1     |
| é      | 2     |
| 你      | 3     |
| 🐻     | 4     |

__
## Що таке rune?

rune — це псевдонім типу int32:

```go
type rune = int32
```
rune представляє одну Unicode-кодову точку, а не байт.
__

## Приклад: байти проти рун

```go
package main

import (
    "fmt"
    "unicode/utf8"
)

func main() {
	const name = "🐻"
	fmt.Printf("constant 'name' byte length: %d\n", len(name))
	fmt.Printf("constant 'name' rune length: %d\n", utf8.RuneCountInString(name))
	fmt.Println("=====================================")
	fmt.Printf("Hi %s, so good to have you back in the arcanum\n", name)
}

```
Вивід:

```
constant 'name' byte length: 4
constant 'name' rune length: 1
=====================================
Hi 🐻, so good to have you back in the arcanum
```
__

## Ітерація по рядку
❌ По байтах (часто неправильно)

```go
for i := 0; i < len(s); i++ {
    fmt.Println(s[i])
}
```
Виводяться сирі байти, а не символи.

✅ По рунах (правильно)

```go
for _, r := range s {
    fmt.Printf("%c\n", r)
}
```
Ключове слово range автоматично декодує UTF-8 у руни.

Виводяться повні символи.
__  

## Коли що використовувати

| Завдання                       | Використовувати          |
| ------------------------------ | ------------------------ |
| Низькорівневі дані / протоколи | `[]byte`                 |
| Обробка тексту                 | `rune`                   |
| Підрахунок символів            | `utf8.RuneCountInString` |
| ASCII-рядки                    | `len()`                  |
| Безпечна ітерація Unicode      | `range`                  |
__

## Типова помилка

```go
len("你好") // 6 ❌ (байти)

```
Правильно:

```go
utf8.RuneCountInString("你好") // 2 ✅ (руни)
```
__

## Висновок

- len(string) рахує байти, а не символи

- Unicode-символи можуть займати кілька байтів

- Для роботи з символами використовуй rune

- Go коректно працює з Unicode, якщо використовувати правильні інструменти
- Ітерація через range — найпростіший спосіб обробки рядків Unicode
Розуміння цих концепцій допоможе уникнути багатьох помилок при роботі з текстом у Go!

## 📘 Пов’язані пакети:
- [unicode/utf8](https://pkg.go.dev/unicode/utf8) — функції для роботи з UTF-8
- [unicode](https://pkg.go.dev/unicode) — утиліти для роботи з Unicode
---