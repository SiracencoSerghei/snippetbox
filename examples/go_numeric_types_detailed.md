# Типи чисел і розміри памʼяті в Go (детально)

## 1. Чому типи та їх розміри важливі

У Go **кожна змінна має чітко визначений тип**, і цей тип:
- визначає **скільки памʼяті** буде використано
- впливає на **продуктивність**
- впливає на **коректність обчислень**
- робить код **безпечнішим**, ніж у динамічних мовах

Go свідомо уникає неявних перетворень типів, щоб запобігти помилкам.

---

## 2. Цілі числа (Integer types)

### 2.1 Знакові цілі числа

| Тип     | Біт | Байти | Діапазон |
|--------|-----|-------|----------|
| int8   | 8   | 1     | -128 … 127 |
| int16  | 16  | 2     | -32 768 … 32 767 |
| int32  | 32  | 4     | -2³¹ … 2³¹-1 |
| int64  | 64  | 8     | -2⁶³ … 2⁶³-1 |
| int    | 32/64 | 4/8 | залежить від архітектури |

📌 **`int` — рекомендований тип за замовчуванням** для лічильників, індексів, циклів.

---

### 2.2 Беззнакові цілі числа

| Тип     | Біт | Байти | Діапазон |
|--------|-----|-------|----------|
| uint8  | 8   | 1     | 0 … 255 |
| uint16 | 16  | 2     | 0 … 65 535 |
| uint32 | 32  | 4     | 0 … 2³²-1 |
| uint64 | 64  | 8     | 0 … 2⁶⁴-1 |
| uint   | 32/64 | 4/8 | залежить від архітектури |
| uintptr | залежить | — | адреси памʼяті |

⚠️ **Уникайте `uint`, якщо можливі відʼємні значення** — це часте джерело багів.

---

## 3. Числа з плаваючою комою (Floating-point)

| Тип     | Біт | Байти | Точність |
|--------|-----|-------|----------|
| float32 | 32 | 4 | ~7 десяткових знаків |
| float64 | 64 | 8 | ~15–16 десяткових знаків |

📌 **`float64` — стандарт у Go**  
Навіть `math` використовує `float64`.

⚠️ **Проблема точності**
```go
fmt.Println(0.1 + 0.2) // 0.30000000000000004
```

👉 Для грошей **НЕ використовувати float** (краще `int64` у копійках).

---

## 4. Комплексні числа

| Тип        | Складається з | Розмір |
|-----------|---------------|--------|
| complex64 | float32 + float32 | 64 біти |
| complex128 | float64 + float64 | 128 біт |

```go
z := complex(3, 4) // 3 + 4i
realPart := real(z)
imagPart := imag(z)
```

📌 Використовуються у:
- фізиці
- DSP
- FFT
- машинному навчанні

---

## 5. Типи за замовчуванням (Best Practice)

| Сценарій | Тип |
|--------|----|
| Лічильники, цикли | int |
| Розміри масивів | int |
| Математика | float64 |
| Комплексна математика | complex128 |
| Гроші | int64 |
| Бінарні дані | uint8 (`byte`) |

---

## 6. Перетворення типів (Type Casting)

### 6.1 Явне приведення

```go
var a int = 10
var b int64 = int64(a)
```

❌ Не можна:
```go
var x int32 = 10
var y int64 = x // compile error
```

---

### 6.2 Float → Int

```go
f := 88.26
i := int64(f)
fmt.Println(i) // 88
```

⚠️ **Дробова частина відкидається, не округлюється**

Для округлення:
```go
math.Round(f)
math.Floor(f)
math.Ceil(f)
```

---

## 7. Переповнення (Overflow)

```go
var x int8 = 127
x++
fmt.Println(x) // -128
```

⚠️ Go **НЕ кидає помилку** при overflow.

---

## 8. Продуктивність і памʼять

- Менший тип ≠ швидший код
- CPU оптимізований під 64-бітні операції
- Передчасна оптимізація — зло

📌 **Читабельність > мікрооптимізація**

---

## 9. Порівняння з Python

| Go | Python |
|---|-------|
| фіксований розмір | довільна точність |
| швидше | повільніше |
| без переповнень перевірки | автоматичне розширення |
| менше памʼяті | більше памʼяті |

---

## 10. Підсумок

✔ Використовуйте `int`, `float64` за замовчуванням  
✔ Уникайте `uint`, якщо не впевнені  
✔ Не використовуйте `float` для грошей  
✔ Явні перетворення — це фіча, не недолік  

---

Автор: Serghei Siracenco  
